########################################
# Core config
########################################

# OpenAI API key for recipe generation (required)
OPENAI_API_KEY=your-openai-api-key
# OpenAI model for recipe generation (required)
OPENAI_MODEL=gpt-5.2
# Strong random string for signing JWTs and securing sessions.
# In production, generate a long random value and keep it secret.
SECRET_KEY=change-me-to-a-long-random-string
